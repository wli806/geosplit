# 想法收集箱(Inbox)

> 用途:做某件事时**临时冒出来、但现在不做**的想法,先丢这里,**不展开**。
> 定期整理进 `数据字典.md` / `AUP-MHU规则数值.md` / `项目进展与决策.md` / `待确认-planner问题.md`,然后从这里删掉。
> 格式随意,一行一个,标日期。

---

## 待整理
（空)

## V2 带过来的洞察(实现对应模块时取用)

- **`domain/geometry.py` 转正**:把地块"长轴转水平"时,要**按地块真实长边自身的角度**转,**不要**用"最小外接矩形最长边"——后者会让沿边的恒定宽度条(车道)在略斜的真实地块上变宽。(Stage 0 实测过的坑。)
- **`domain/geometry.py` 边归一化**:做"按边"逻辑前,**近共线的两条边按角度容差(±2~3°)合并成一条**,否则把一条近似直边当两条会算错。
- **`sources/_cache.py` 缓存**:取数结果确定性 → 该缓存,key 带数据版本号;底层数据更新给 TTL;开发期可绕过。(Stage 0 踩过"缓存没带版本号导致看旧结果"的坑。)
- **`domain/envelope.py` 未来:公共/开放空间邻居免 HIRB**(H5.6.5(2):邻接 >2000㎡ 且 >20m 宽的开放空间/河流/商业区,该侧 HIRB 不适用)。MVP 默认除道路外三侧都算 HIRB;以后接"邻地 zone"后自动识别免除。

## 已整理(可删)
- E38 细分要点 → 已并入 `AUP-MHU规则数值.md`。
